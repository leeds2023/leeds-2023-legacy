---
import { clsx } from 'clsx';
type Props = {
	size?: 'small' | 'medium' | 'large';
};
const { size = 'medium' } = Astro.props;
---

<div
	class={clsx(size === 'small' && 'h-8', size === 'medium' && 'h-16', size === 'large' && 'h-24')}
>
</div>
