---
import { storyblokEditable } from '@storyblok/astro';

import type { PartnersStoryblok } from '@/lib/storyblok/types';
type Props = {
	blok: PartnersStoryblok;
};
const { blok } = Astro.props;
---

<div {...storyblokEditable(blok)}>
	{JSON.stringify(blok.component)}
</div>
