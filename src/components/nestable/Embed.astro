---
import { storyblokEditable } from '@storyblok/astro';

import type { EmbedStoryblok } from '@/lib/storyblok/types';
type Props = {
	blok: EmbedStoryblok;
};

const { blok } = Astro.props;
---

<div {...storyblokEditable(blok)}>
	<p>{JSON.stringify(blok.component)}</p>
</div>
