---
import Render from '@/lib/storyblok/Render.astro';
import { fetchStory } from '@/lib/storyblok/helpers';
const { content } = await fetchStory('an-international-leeds');
---

<Render blok={content} />
